// all the packages that we have yet installed are not global packages. they are only accessible from the from where they have installed. some packages are also global packages. they can be accessible from any folder. they are called global packages. npm is its example.
// npm is a command line tool which can be running from any where.
// we use -g flag to install a package globaly.
// to list the global packages we use 'npm list -g'
// npm -g outdated // used to check the outdated versions.
// npm -g update // used to update the globall packages