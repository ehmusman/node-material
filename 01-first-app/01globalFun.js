// const sayHello = (name) => {
//     console.log('Hello : ' + name)
// }
// sayHello('usman'); 
// // console.log(window)

/////////////////////////////////
// console.log() // to log something on the console. console is a global object (global scope) we can access it from anywhere from any file. there are also many other objects which are available globally like
// setTimeout() // it call a function after some delay we can use it inclient side as well as server side.
// clearTimeout()// its also a global scope
// setInterval() // used to repeatedly call a function after some time of delay
// clearInterval() // used to stop the above functions to stop it to calll a function repeatedly

// in javascript window object is used. it can be work withh all global objects like window.console.log() etc.

// var message  = '';
// console.log(window.message)// not a global object due to node modular system.
// in short we can use "global" object with any global object
